import { RegisterForm } from "../components/RegisterForm";

export const metadata = {
  title: "Create Account - AssetTrack",
  description: "Create your AssetTrack account",
};

export default function RegisterPage() {
  return <RegisterForm />;
}
